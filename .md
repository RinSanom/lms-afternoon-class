how set project
1. npm init 
2. npm install express
3. npm install -D typescript @types/node @types/express ts-node-dev
4. npx tsc --init
5. npm i -g nodemon  /// reload, automatically.


npm install @prisma/adapter-pg pg
npm install -D @types/pg

